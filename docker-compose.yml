services:
  gateway:
    container_name: kiro-gateway
    build: .
    ports:
      - "${WEB_PORT:-3000}:3000"
    env_file: .env
    environment:
      DATABASE_PATH: /app/data/gateway.db
      KIRO_CLI_DB_FILE: /app/kiro-cli/data.sqlite3
      KIRO_CREDS_FILE: /app/creds/kiro-auth-token.json
    volumes:
      - ./data:/app/data
      - ${KIRO_CREDS_HOST_PATH:-${HOME}/.aws/sso/cache}:/app/creds
      - ${KIRO_CLI_HOST_PATH:-${HOME}/.local/share/kiro-cli}:/app/kiro-cli
    restart: unless-stopped
